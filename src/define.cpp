#include "lang.h"
#include <fstream>
#include <cassert>

#define EOL (-2)

namespace Lang {
	//Keywords = { "fn", "if", "else", "var", "const", "return", "struct", "impl", "for", "in", "where", "continue", "break", "self", "interface", "true", "false" };
	//vector<string> Operators = { "!", "-" };

	
}